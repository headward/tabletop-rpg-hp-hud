export class Player {
    constructor(public name: string,
                public hp: string,
                public maxHp: string) {
        this.name = name;
        this.hp = hp;
        this.maxHp = maxHp;
    }
}
